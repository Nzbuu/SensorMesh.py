sudo: false
language: python
python:
  - 3.5
install:
  - pip install --disable-pip-version-check --upgrade pip
  - pip install -r requirements.txt
  - pip install python-coveralls
script:
  - python -m pytest --cov=sensormesh
after_success:
  - coveralls
